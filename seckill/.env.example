DB_HOST=8.148.75.17
DB_PORT=3306
DB_USER=wy-flj
DB_PASSWORD=your_password
DB_NAME=wy-flj

# 开抢提前量（毫秒）：相对 seckillStartTime 往前偏移后再发首枪 acquire
# 例：开抢 12:00:00.000 + FIRE_EARLY_MS=100 → 约 11:59:59.900 发请求
FIRE_EARLY_MS=100

# 连续抢购间隔（毫秒），0=上一响应返回后立即下一发
ACQUIRE_INTERVAL_MS=0

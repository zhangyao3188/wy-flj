DB_HOST=8.148.75.17
DB_PORT=3306
DB_USER=wy-flj
DB_PASSWORD=your_password
DB_NAME=wy-flj

# 开火偏移区间（毫秒）：实际开火 = 开抢时间 + 随机偏移
# 负数=提前，正数=延后；多账号各自在区间内随机
# 例：[0,100] → 开抢后 0~100ms；[-100,200] → 提前100ms ~ 延后200ms
FIRE_EARLY_MS=[0,100]

# 连续抢购间隔（毫秒），0=上一响应返回后立即下一发
ACQUIRE_INTERVAL_MS=0
